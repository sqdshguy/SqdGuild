# SqdGuild
Repository with an example usage of VimeWorld API.
# Setup
᛫ Install all dependencies using `npm i`
᛫ Change all guild IDs in code
᛫ Run it! Just use `npm start` / `node .` / `node server.js`
